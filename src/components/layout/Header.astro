---
import MobileNav from './MobileNav.astro';

const { currentPath = '/' } = Astro.props;
const desktopNav = [
  { label: 'Leistungen', href: '/leistungen' },
  { label: 'Wissen',      href: '/wissen' },
  { label: 'Kontakt',    href: '/kontakt' },
];
---
<header class="sticky top-0 z-30 bg-surface/95 backdrop-blur border-b border-border">
  <div class="max-w-content mx-auto px-4 sm:px-6 lg:px-8 flex items-center justify-between h-16 lg:h-20">
    <!-- Logo -->
    <a href="/" class="flex items-center gap-2">
      <span class="text-lg lg:text-xl font-bold text-primary">Dachgeschoss</span>
      <span class="text-lg lg:text-xl font-light text-secondary">ausbau</span>
      <span class="text-xs font-medium text-text-secondary hidden sm:inline">München</span>
    </a>

    <!-- Desktop Navigation -->
    <nav class="hidden lg:flex items-center gap-8">
      {desktopNav.map(item => (
        <a
          href={item.href}
          class={`text-sm font-medium transition-colors ${
            currentPath.startsWith(item.href)
              ? 'text-primary'
              : 'text-text-secondary hover:text-text-primary'
          }`}
        >
          {item.label}
        </a>
      ))}
      <a href="/kontakt" class="cta-primary text-sm px-6 py-3">
        Beratung anfragen
      </a>
    </nav>

    <!-- Mobile Nav -->
    <MobileNav currentPath={currentPath} />
  </div>
</header>